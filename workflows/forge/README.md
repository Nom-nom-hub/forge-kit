# FORGE Workflows

Forge Kit includes composable workflows that automate the FORGE development lifecycle.

## Workflow: Signal-Driven Development

1. `/forge.signal` — Capture a signal
2. `/forge.hypothesize` — Form a hypothesis
3. `/forge.decide` — Make a decision
4. `/forge.feature` — Create a feature
5. `/forge.experiment` — Design an experiment
6. `/forge.release` — Ship a release
7. `/forge.retrospect` — Learn and loop back
